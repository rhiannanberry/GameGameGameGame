using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[ExecuteInEditMode]
public class StyleManager : MonoBehaviour
{
    [SerializeField] private ColorPaletteSO activePalette = null;
    public ColorPaletteSO ActivePalette { get { return activePalette; } }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
